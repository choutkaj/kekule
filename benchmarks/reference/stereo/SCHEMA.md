# Retired stereo snapshots

The compressed stereo schema-v2 files are historical artifacts. Some inherited
implementation/manual assertions, so they are not independent goldens and are
not used by the current runner. Generate fresh RDKit results with the commands
in [the benchmark guide](../../GUIDE.md).
