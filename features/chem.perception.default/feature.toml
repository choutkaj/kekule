id = "chem.perception.default"
title = "Default discrete perception pipeline"
area = "chemistry"
domains = ["small-molecule"]
version = 2
status = "supported"
description = "Transactionally install the default valence, ring-set, and aromaticity state, with an atomic SmallMolecule convenience that explicitly composes normalization and perception."
depends_on = ["chem.normalization", "algo.valence.rdkit-like", "algo.rings.sssr", "algo.aromaticity.rdkit-like"]
