id = "io.trajectory.dcd"
title = "DCD trajectory codec"
area = "io"
domains = ["infrastructure"]
version = 8
status = "supported"
description = "Read and write the implemented common CHARMM, NAMD, and OpenMM DCD profile with checked records, cells, fixed atoms, indexing, and explicit time policy."
depends_on = ["io.trajectory.file"]
