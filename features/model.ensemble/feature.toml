id = "model.ensemble"
title = "Shared-topology structural ensembles"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 5
status = "supported"
description = "Store finite non-temporal model members with direct positions, cells, and AtomData that share one exact topology and remap through explicit topology lineage."
depends_on = ["model.topology", "model.system"]
