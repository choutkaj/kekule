id = "model.ensemble"
title = "Shared-topology structural ensembles"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 4
status = "supported"
description = "Store finite non-temporal configurations and observation state that share one exact topology and remap them through explicit topology lineage."
depends_on = ["model.topology", "model.system"]
