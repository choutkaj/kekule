id = "area.name"
title = "Human-readable feature title"
area = "core"
domains = ["small-molecule"]
version = 1
status = "planned"
description = "One-sentence description."
depends_on = []

# reference = ["rdkit.Chem.example"]
# public_api = ["kekule::path::function"]
