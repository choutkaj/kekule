id = "model.system"
title = "Topology-bound molecular model"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 14
status = "supported"
description = "Represent one concrete molecular model over connected molecule instances as an immutable Topology plus direct positions, optional cell, and topology-bound AtomData, with checked lineage remapping."
depends_on = ["model.topology", "core.conformers", "core.units"]
