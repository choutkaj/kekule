id = "model.system"
title = "Topology-bound molecular model"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 8
status = "supported"
description = "Represent one concrete molecular model as an immutable Topology plus one validated Configuration and optional observation state."
depends_on = ["model.topology", "core.conformers", "core.units"]
