id = "model.system"
title = "Topology-bound molecular model"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 10
status = "supported"
description = "Represent one concrete molecular model as an immutable Topology plus one validated Configuration and optional observation state, with explicit checked topology-lineage remapping."
depends_on = ["model.topology", "core.conformers", "core.units"]
