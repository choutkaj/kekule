id = "stereo.representation"
title = "Stereochemistry representation"
area = "stereo"
domains = ["small-molecule"]
version = 15
status = "supported"
description = "Store first-class stereo elements, exact stable relation-group slot layouts, source bond marks, derived descriptor vocabulary, and atom, hydrogen, or lone-pair stereo carriers adjacent to the molecular graph, with PubChem, Enamine, and PL-REX semantic regression coverage."
depends_on = ["core.graph", "core.atom-bond"]
