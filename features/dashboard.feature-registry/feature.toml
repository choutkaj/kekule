id = "dashboard.feature-registry"
title = "Feature registry and dashboard"
area = "infrastructure"
domains = ["infrastructure"]
version = 15
status = "supported"
description = "Keep release-status and dependency metadata as the source of truth, validate the feature graph, and render optional benchmark availability and observations without treating them as feature health."
depends_on = []
