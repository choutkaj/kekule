id = "chem.normalization"
title = "Small-molecule representation normalization"
area = "chemistry"
domains = ["small-molecule"]
version = 4
status = "supported"
description = "Deterministically and transactionally rewrite asserted small-molecule chemistry, imported aromatic bond orders, and source stereo marks into Kekule's canonical represented form while clearing derived perception."
depends_on = ["core.graph", "core.atom-bond", "stereo.representation"]
