id = "chem.normalization"
title = "Small-molecule representation normalization"
area = "chemistry"
domains = ["small-molecule"]
version = 2
status = "supported"
description = "Deterministically and transactionally rewrite asserted small-molecule chemistry, including imported aromatic bond orders, into Kekule's canonical represented form while clearing derived perception."
depends_on = ["core.graph", "core.atom-bond"]
