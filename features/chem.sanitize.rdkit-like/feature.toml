id = "chem.sanitize.rdkit-like"
title = "RDKit-like sanitization pipeline"
area = "chemistry"
domains = ["small-molecule"]
version = 22
status = "supported"
description = "Transactionally normalize chemistry and source stereo, then install requested valence, ring, and aromaticity state plus run optional stereo validation."
depends_on = ["chem.normalization", "algo.valence.rdkit-like", "algo.rings.sssr", "algo.aromaticity.rdkit-like", "stereo.perception"]
