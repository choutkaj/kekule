id = "chem.sanitize.rdkit-like"
title = "RDKit-like sanitization pipeline"
area = "chemistry"
domains = ["small-molecule"]
version = 19
status = "supported"
description = "Transactionally normalize represented small-molecule chemistry and install requested valence, ring, aromaticity, and stereo perception."
depends_on = ["algo.valence.rdkit-like", "algo.rings.sssr", "algo.aromaticity.rdkit-like", "stereo.perception"]
