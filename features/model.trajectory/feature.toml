id = "model.trajectory"
title = "Shared-topology trajectory streaming"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 10
status = "supported"
description = "Provide kekule-traj as the fixed-topology trajectory layer for ordered frames, reusable buffers, streaming, file I/O, and trajectory-oriented workflows."
depends_on = ["model.topology", "model.system", "core.units"]
