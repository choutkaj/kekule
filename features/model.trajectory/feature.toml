id = "model.trajectory"
title = "Shared-topology trajectory streaming"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 6
status = "supported"
description = "Represent ordered fixed-topology frames, publish and remap complete frame state transactionally, and stream through reusable topology-bound buffers."
depends_on = ["model.topology", "model.system", "core.units"]
