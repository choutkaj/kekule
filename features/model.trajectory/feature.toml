id = "model.trajectory"
title = "Shared-topology trajectory streaming"
area = "modeling"
domains = ["small-molecule", "macromolecule"]
version = 4
status = "supported"
description = "Represent ordered fixed-topology frames, remap complete frame state explicitly, and stream through reusable topology-bound buffers."
depends_on = ["model.topology", "model.system", "core.units"]
