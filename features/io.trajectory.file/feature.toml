id = "io.trajectory.file"
title = "File-backed trajectory I/O"
area = "io"
domains = ["infrastructure"]
version = 2
status = "experimental"
description = "Open, detect, index, stream, and atomically write bounded fixed-topology trajectory files through Molecular trajectory contracts; the initial XYZ vertical slice is usable."
depends_on = ["model.trajectory", "core.units"]
