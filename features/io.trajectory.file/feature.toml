id = "io.trajectory.file"
title = "File-backed trajectory I/O"
area = "io"
domains = ["infrastructure"]
version = 8
status = "supported"
description = "Open, detect, index, stream, and atomically write supported bounded XYZ, DCD, TRR, and XTC profiles through the kekule-traj trajectory contracts."
depends_on = ["model.trajectory", "core.units"]
