id = "io.trajectory.file"
title = "File-backed trajectory I/O"
area = "io"
domains = ["infrastructure"]
version = 4
status = "supported"
description = "Open, detect, index, stream, and atomically write the supported bounded XYZ, DCD, TRR, and XTC fixed-topology profiles through Molecular trajectory contracts."
depends_on = ["model.trajectory", "core.units"]
